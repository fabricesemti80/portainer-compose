# Secrets for MinIO
# These environment variables should be stored securely (e.g., in Docker secrets or external secret management)

MINIO_ROOT_USER=your_secure_minio_user
MINIO_ROOT_PASSWORD=your_secure_minio_password
